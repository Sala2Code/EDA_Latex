#ifndef COMPUTE_H
#define COMPUTE_H

void compute_col(const char *path);
void compute_cat_col(int i, char** arr);
void compute_cont_col(int i, char** token);
void compute_cont_col(int i, char** arr);

int compare(const void *a, const void *b);

#endif